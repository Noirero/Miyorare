name: PF5 Private AssistContent Fix

on:
  push:
    branches:
      - pf5

permissions:
  contents: write

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
      - name: Keep Private AssistContent scrubbed with screenshots enabled
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/pf5_private_assist_patch.py
          git diff --check
          git status --short
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main
          git commit -m 'Keep Private AssistContent scrubbed with screenshots enabled'
          git push origin HEAD:pf5
