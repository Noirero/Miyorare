package org.koitharu.kotatsu.core.prefs

enum class DetailsUiMode {
	/** Compose layout with a large cover centered above the title (showcase style). */
	EXPRESSIVE,

	/** Compose layout with a compact cover beside the title and info; this is the default mode. */
	COMPACT,
}
