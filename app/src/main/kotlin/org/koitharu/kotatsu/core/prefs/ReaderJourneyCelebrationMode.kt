package org.koitharu.kotatsu.core.prefs

/**
 * Controls Reader Journey feedback only. XP, ranks and cosmetic unlocks are unaffected.
 */
enum class ReaderJourneyCelebrationMode {
	OFF,
	SUBTLE,
	FULL,
}
