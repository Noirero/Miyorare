<?xml version="1.0" encoding="utf-8"?>
<!-- One Explore pager page (manga or novel extensions). The page fills the bounded pager viewport and
     keeps its RecyclerView scrollable/nested so large source libraries remain virtualized instead of
     inflating every row just to emulate wrap_content height. -->
<FrameLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent">

	<androidx.recyclerview.widget.RecyclerView
		android:id="@+id/recyclerView"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:clipToPadding="false"
		android:nestedScrollingEnabled="true"
		android:orientation="vertical"
		tools:listitem="@layout/item_explore_source_list" />

</FrameLayout>
